export default function Catalog() {
  return (
    <div>
      <div
        className="fixed inset-0 z-10 flex items-center justify-center backdrop-blur-sm" 
        // aria-labelledby={`${id}-label`} // ระบุ label สำหรับ Screen Reader
      >
      <div className="w-full   bg-box rounded-lg shadow-lg ">
<h1></h1>
      </div>
      </div>
    </div>
  );
}
